name: Keep site clean
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cloud sync is embedded in index.html; no patching required."
